**/.circleci
**/.editorconfig
**/.dockerignore
**/.git
**/.DS_Store
**/.vscode
**/node_modules

# Specific patterns to ignore
data-node
meili_data*
librechat*
# Keep the committed runtime config in the image (endpoints, interface, etc.).
# Must come after `librechat*` so the negation wins.
!librechat.yaml
Dockerfile*
docs

# Ignore all hidden files
.*
